the GNU project and Linux are very cool

print working directory, aka Where am I
$ pwd

change directory, aka moving around
$ cd, cd -  

make directory, put my stuff somewhere
$ mkdir 

when naming stuff no spaces, special characters, lower case, camel casing  

list, what is in a folder
$ ls

open or update a file!
$ touch file.txt

nano/for loop/declare a variabel
#!/bin/bash
variable="your name"
for i in 1 2 3 4 5
do
  touch $i.txt
done
echo $variable

erase files 
#!/bin/bash
for i in $(seq 4374 2 4420);
do
  rm "IMG_"$i"_ocr.txt"
done

make a program executable 
chmod a+x FILENAME
./FILENAME

mv with wild card 

Get a web page and use regular expression 
$ ls -l | grep "Final" 
$ grep "Trump" outputfile.txt > Trumpfile.txt
